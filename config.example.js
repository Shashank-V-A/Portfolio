// Duplicate this file as `config.js` (at the project root) and never commit it.
// Add your real Gemini API key there so it stays out of version control.
window.__PORTFOLIO_CONFIG__ = {
    GEMINI_API_KEY: "YOUR_GEMINI_API_KEY_HERE"
};

