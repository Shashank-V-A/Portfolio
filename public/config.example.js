// Duplicate this file as `config.js` in the public folder before deploying.
// Fill in your real Gemini API key there; this placeholder stays in git.
window.__PORTFOLIO_CONFIG__ = {
    GEMINI_API_KEY: "YOUR_GEMINI_API_KEY_HERE"
};

